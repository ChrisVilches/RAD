require "rails_helper"

RSpec.describe QueriesController, type: :controller do

  pending "doing @query.save also saves the container"
  pending "query history doesn't get updated if the code is the same"

end
